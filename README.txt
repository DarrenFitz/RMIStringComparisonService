Darren Fitzpatrick
G00311853@gmit.ie

An Asynchronous RMI String Comparison Service

Run Project:
- Import the comparator.war file into eclipse and this creates the project.
- To run the server, go to directory with string-service.jar file run the command:
  java -cp ./string-service.jar ie.gmit.sw.Servant
- to see rmi working, runn the client class

Other:
- Got the rmi working but but only locally
- linked list not working for in & out queues
  was unable to complete it.



